# Docker Environment Variables
# This file contains Docker-specific environment variables

# =============================================================================
# PATH CONFIGURATION FOR DOCKER
# =============================================================================

# Data root inside container (usually should not be changed)
DATA_ROOT=/app/data

# Project root inside container (usually should not be changed)  
PROJECT_ROOT=/app

# Python path for imports
PYTHONPATH=/app

# =============================================================================
# PROCESSING CONFIGURATION
# =============================================================================

# Default region and date for processing
REGION_NAME=BinhDinh_20171110
DETECTION_DATE=20171110

# Training parameters
BATCH_SIZE=8
IMG_HEIGHT=256  
IMG_WIDTH=256
EPOCHS=50

# Thresholds
MORPH_THRESHOLD=0.6
THRESH_PRED=0.5
THRESH_LABEL=0.5
DELTA_M=5.0
JRC_THRESHOLD=99

# =============================================================================
# GPU AND PERFORMANCE SETTINGS
# =============================================================================

# CUDA settings
CUDA_VISIBLE_DEVICES=0
TF_CPP_MIN_LOG_LEVEL=1

# Memory settings (uncomment if needed)
# TF_FORCE_GPU_ALLOW_GROWTH=true
# GPU_MEMORY_LIMIT=8192

# Number of workers for data loading
NUM_WORKERS=4

# =============================================================================
# JUPYTER CONFIGURATION  
# =============================================================================

# Jupyter settings (for development)
JUPYTER_ENABLE_LAB=yes
JUPYTER_TOKEN=
JUPYTER_PASSWORD=

# =============================================================================
# TENSORBOARD CONFIGURATION
# =============================================================================

# TensorBoard settings
TENSORBOARD_LOG_DIR=/app/training_logs